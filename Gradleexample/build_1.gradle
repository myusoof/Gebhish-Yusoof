task testTaskOtherFile << {
   println "test task other file"
    doLast{
        println "test the task last"
    }
    println "test the task 2"
}

task taskFromNewFile <<{
    println "Task from new file"
}


task compile1 << {
    println "compiling one"
}
task building1 (dependsOn : compile1) << {
    println "building one"
}

task publish1 (dependsOn: [compile1, building1]) << {
    println "publishing one"
}

task copyTask(type : Copy){
    from 'src'
    into 'dest'
}

task testProperties111 << {
    println key1
}task countOtherFile (dependsOn : testTaskOtherFile) << {
    4.times { println "$it" }
}

countOtherFile << {
    println "task appended"
}



4.times{ counter ->
    task "taskNewFile${counter}" << {
        println "tasks new file ${counter}"
    }
}

task extraProperty << {
    ext.myProperty = "yusoof extra property"
}

task testExtraProperty << {
   println extraProperty.myProperty
}

//---------------------------------------------------------------------------------//
task compile1 << {
    println 'compiling source'
}

task compileTest1(dependsOn: compile1) << {
    println 'compiling unit tests'
}

task test1(dependsOn: [compile1, compileTest1]) << {
    println 'running unit tests'
}

task dist1(dependsOn: [compile1, test1]) << {
    println 'building the distribution'
}
