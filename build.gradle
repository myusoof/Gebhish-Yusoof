apply plugin : 'groovy'
apply plugin : 'java'
apply plugin : 'idea'
apply from : 'external-dependencies.gradle'

allprojects{
    repositories{
        mavenCentral()
    }
}

dependencies{
    compile  groovy, gebhish, seleniumFirefox, seleniumSupport, testng
}